# 示例与能力审计

日期：2026-09-12。范围为20个自有摄影Skill。处理结果来自实际Python执行，风格结果来自实际生成；合成素材、手工深度与模拟多帧均明确标注。

## 一、覆盖范围

- 14个Python Skill各保留工作台基础例，并新增动物、城市、人物3组受控案例，共42组新增执行。
- 另补3组显式曝光与白平衡校准、3组主体保护裁切，以及1组人脸候选确认例。
- 6个生图Skill各新增2种主题，连同既有示例，每类至少3例。微缩建筑、动物和钢笔另做定向修正。
- 原始文件通过SHA-256核对；已查看14组三题材对照板、校准结果、保护裁切、风格结果及3份实际PDF的栅格化结果。
- 使用既有Python环境；为符合已声明依赖，在任务独立目录加载OpenCV4.11，没有降级或覆盖共享环境。

这些数字是工作流案例，不是独立实拍数据集规模。三组打印及排序案例复用相同照片组合，不能宣称三套独立项目验收。

## 二、由测试推动的升级

| Skill | 已实现的改变 | 实测证据 |
|---|---|---|
| photo-color-grade | 新增calibrate_photo，支持显式EV、线性RGB增益或确认的中性采样区；不放宽自动白平衡阈值 | 已知偏差逆校准3例PSNR53.03至53.20dB，保留原图和复核状态 |
| photo-bracket-fusion | 运动判断前做逐通道单调分位数光度补偿，排除无效边界 | 3题材静态曝光残差比例0；另测伽马变化、不同帧局部运动和无效边界，保留真实变化检测 |
| photo-composition-crop | 新增protected_region硬约束；不可容纳时写文件前拒绝 | 检查动物头尾、人物头部与可见身体；无效坐标和不可能画幅均拒绝 |
| photo-to-3d | 网格校验改为按实际宽高比核对顶点、面片、UV和索引 | 原来被错误拒绝的64长边横图恢复；横图、竖图、宽幅与方图均有回归 |
| photo-privacy-redactor | 明确检测器可用状态与OpenCV版本，区分缺依赖和零候选 | 兼容构建检出人脸，同时如实保留衣袖、椅子误报；确认例仅接受已复核框 |
| photo-miniature-diorama | 明确裁断建筑的剖切终止面、动物承托和包含外轮廓的留白检查 | 去掉运河首版无依据屋顶；赤狐四足与尾部保留，留白改善 |
| object-exploded-plate | 按组件组而非颜色核对拆解空隙 | 钢笔首版漏拆握位与笔尖，定向修正后五组可读 |

## 三、仍须保留的边界

- 自动校色不是已知参数逆运算。没有可靠中性色时维持保守结果；新增人工校准不能恢复已经剪切的信息。
- 降噪仍有细毛和叶片的平滑取舍；没有把模拟噪声测试扩大成RAW、镜头或全部传感器认证。
- 局部光影及本轮3D使用手工蒙版或深度，不能冒充自动分割或自动估深。自动估深模型下载仍须用户许可，本轮未完成该路径的实图验证。
- 裁切的旧焦点草案中部分画幅会切断主体，新保护区域例解决明确范围的保留问题，但不等于自动人体识别。
- 新交付质检例故意使用1152长边输入，低于1200阈值，blocked为正确负例；另导出800像素副本不改变该规格判定。
- 隐私候选可以误报和漏报。候选确认例仍为needs-manual-review，没有全图漏检的用户确认，也未处理元数据隐私。
- 多帧曝光、焦点和全景均为受控模拟，不证明真实拍摄视差、对焦呼吸、运动或物理HDR恢复。
- 3D输出是开放正面高度场，粗深度仍有低结构相关警告。不是完整360度物体、米制扫描或可直接打印的封闭网格。
- PDF已实际渲染检查，但输入缺ICC的警告仍保留，不称为印厂CMYK认证。
- 风格图片已做视觉检查，仍属创意图片：地图不能导航，爆炸图不能指导制造，蓝晒不能用于植物鉴定。没有承诺跨模型逐像素一致。

## 四、目录与发布

本地全量回归结果：208项通过，包含79项原有算法及海报测试、基础与扩展示例检查、新增能力行为回归及发布结构检查。测试源文件不发布，读者可使用随附复现函数和验证记录检查案例。

保留必要脚本、参考资料、输入、输出、提示和复现函数。旧缓存、会话文件、无关PDF模板及被替代输出有恢复备份；没有永久删除用户素材。测试源文件只留本地，不随Git发布。高位深TIFF中间文件同样不发布，可由随附函数重新生成。

公开JSON与CSV按报告所在目录使用相对路径。仓库不携带厂商适配文件或大型模型权重。测试断言通过、视觉复核、发布状态分别记录，不以退出码替代业务验收。
